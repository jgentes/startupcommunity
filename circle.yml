deployment:
  production:
    branch: staging
    commands:
      - git add . -A; git commit -am "commit for heroku push"
      - git push origin staging:master
      - git push git@heroku.com:startupcommunity.git staging:master