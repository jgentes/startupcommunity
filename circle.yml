deployment:
  production:
    branch: staging
    commands:
      - ./build.sh
      - git add . -A
      - git commit -am "Push staging to master"      
      - git push git@heroku.com:startupcommunity.git $CIRCLE_SHA1:master
      - git push jgentes@github.com:startupcommunity.git $CIRCLE_SHA1:master